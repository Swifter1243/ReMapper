/** The color for a note. */
export enum NoteColor {
    RED = 0,
    BLUE = 1,
}

/** Note cut directions. */
export enum NoteCut {
    UP,
    DOWN,
    LEFT,
    RIGHT,
    UP_LEFT,
    UP_RIGHT,
    DOWN_LEFT,
    DOWN_RIGHT,
    DOT,
}
